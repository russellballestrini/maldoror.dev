# Regional route materials

These source materials are a bounded direction study generated through the
Codex built-in ChatGPT subscription path. The original atlas, exact prompt,
hash, semantic manifest, and derived masters are retained together.

`pnpm assets:derive-routes` detects the explicit magenta gutters and derives the
four 512×512 masters. Runtime code consumes only manifest semantics; it never
classifies filenames or pixels.

The materials are research candidates until they pass the faithful regional
octant lab and physical Ghostty direction approval.
