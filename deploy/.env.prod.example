POSTGRES_PASSWORD=change_this_to_a_secure_password
AI_PROVIDER=anthropic
ANTHROPIC_API_KEY=sk-ant-xxx
# Or use OpenAI:
# AI_PROVIDER=openai
# OPENAI_API_KEY=sk-xxx
AI_MODEL=claude-sonnet-4-20250514
# Delayed SSH compression is required by default. Set this only as a temporary
# compatibility rollback for a client that cannot offer zlib@openssh.com.
# MALDOROR_SSH_COMPRESSION=optional
